<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.davidbronn.fragmentrevision">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_to_fragment.MainActivity"/>
        <activity android:name=".fragment_to_activity.MainActivity2" />
        <activity android:name=".inter_fragments.MainActivity3" />
        <activity android:name=".child_fragments.ParentActivity" />
        <activity android:name=".fragment_states.BaseActivity" />
        <activity android:name=".fragment_states.MainActivity" />
        <activity android:name=".broadcast_communicate.BroadcastActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>